<!-- auto-index -->
# INDEX

> JSON-LD context definitions — prefix mappings, vocab bindings, and namespace declarations used across the linked data graph

## Files
| Name | Description |
|------|-------------|
| `_.jsonld` | Catalog definition for "Atlas Contexts" under the Atlas root catalog |
| `dcat.jsonld` | DCAT and Dublin Core Terms namespace prefix bindings |
| `nexus.jsonld` | Nexus-specific prefix mappings (`atlas:`, `core:`) and Nexus ontology vocab binding |
| `space.jsonld` | Inner/outer spatial namespace prefix bindings |
