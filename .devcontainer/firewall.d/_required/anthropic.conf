# Anthropic API — required for Claude Code to function at all.
# Telemetry endpoints (sentry, statsig) are NOT required and live in
# _optional/anthropic-telemetry.conf — Claude Code works without them.
api.anthropic.com
