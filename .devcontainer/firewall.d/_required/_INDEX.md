<!-- auto-index -->
# INDEX

> Universal hard-required firewall allowlists — hard-fail on a DNS miss when selected; selected via FIREWALL_KERNEL_MODULES (default: all); a DNS resolution failure here aborts the container boot

## Files
| Name | Description |
|------|-------------|
| `github.conf` | GitHub API, raw content, Git object storage, and LFS upload endpoints |
| `npm.conf` | npm registry — required for Claude Code install and Node package operations |
