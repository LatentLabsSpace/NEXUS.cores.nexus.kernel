# Claude Code telemetry — optional egress. Claude Code degrades silently and
# harmlessly when these are unreachable, so a DNS miss must not block boot.
#
# statsig.anthropic.com is intentionally absent: it went NXDOMAIN as of
# 2026-07-18. It previously sat in _required/anthropic.conf and hard-failed
# every downstream consumer's boot. Do not re-add it.
sentry.io
statsig.com
